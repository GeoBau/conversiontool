if an artnr is new "Hinzufügen" we have 2 cases
Item:
    if the inputfield fulfills hte formal nees (item x.x.x.x)
    then "prüfen" becomes active
        when Prüfen pressed in the background the artnumber will be tested
        the artnr will be passed to https://www.item24.com/de-de/search/?q=0.0.439.33
        q= artnr in the result will becheck if "1 Treffer" appears.
        <div class="sort__col sort__col--counter col">
                    1 Treffer
                </div>
        if ok activate Speichern button
Alvaris:
    if the inputfield fulfills hte formal needs (7numbers)
        when Prüfen pressed in the background the artnumber will be tested
        the artnr will be passed to https://www.alvaris.com/de/?s=1010073&trp-form-language=de
        s=artnr. The result will becheck if artnr is there.
            <a class="uk-link-reset" href="https://www.alvaris.com/de/2022/04/22/1010073-antirutschplatte-fuer-stellfuss-80/">1010073-Antirutschplatte-für-Stellfuss-80</a>
        if ok activate Speichern button
Bosch:
    if the inputfield fulfills hte formal needs (10numbers)
        when Prüfen pressed in the background the artnumber will be tested
        the artnr will be passed to https://www.boschrexroth.com/de/de/search.html?q=3842538559
        q=artnr. The result will becheck if "1 ERGEBNISSE" is there.
<span class="bundle">1 ERGEBNISSE</span>
        if ok activate Speichern button
ASK:
    if the inputfield fulfills hte formal nees (8numbers)
    then button "man.Test" and "speichern" will become active
        when "man.Test" is pressed the testpages for ASK https://shop.askgmbh.com/auctores/scs/imc will be opened
        